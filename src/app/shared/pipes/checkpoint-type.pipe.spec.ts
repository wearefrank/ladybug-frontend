import { CheckpointTypePipe } from './checkpoint-type.pipe';

describe('CheckpointTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckpointTypePipe();
    expect(pipe).toBeTruthy();
  });
});
